package org.oki.elv.common.face.step.preparation;

/**
 * Selection mode of prepared data.
 * @author Elv
 */
public enum SELECTION {
  /** A selection type. */
  NOT_DISTINCT,
  /** A selection type. */
  DISTINCT
}
